# Server-only environment for Express upload API
# Copy this to .env.server and fill with your credentials

# Firebase Admin (server only)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Paste the private key with literal \n or real line breaks; both are supported
FIREBASE_PRIVATE_KEY=

# Supabase (server only)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Storage settings
STORAGE_BUCKET=profiles
# Use signed URLs if bucket is private
USE_SIGNED_URLS=false
SIGNED_URL_TTL=86400

# Server
PORT=4000

# Notes:
# - Keep this file OUT of client builds. CRA ignores .env.server by default.
# - Never expose SUPABASE_SERVICE_ROLE_KEY in the browser.
# - On Windows, keep private key as one line with \n between lines or use real multiline value.